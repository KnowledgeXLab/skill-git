# Website v1.1 Update Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update index.html on gh-pages to reflect v1.1 commands (search, install, list, delete) across the Commands, Features, and Best Practices sections, with full i18n (en + zh).

**Architecture:** Single-file edit to `index.html`. Changes are grouped by section to keep diffs readable and commits atomic. i18n dictionary is updated last so all new keys are defined before the JS that consumes them.

**Tech Stack:** Static HTML/CSS/JS, no build step. Browser opens `index.html` directly for verification.

**Spec:** `docs/2026-04-09-website-v1.1-update-design.md`

---

### Task 1: Update Commands section — title + 4 new rows

**Files:**
- Modify: `index.html` (Commands section, ~line 301–312)

- [ ] **Step 1: Update section title in HTML**

Find and replace in `index.html`:
```html
<h2 class="section-title fade-up" data-i18n="commands.title">Six commands. Full control.</h2>
```
→
```html
<h2 class="section-title fade-up" data-i18n="commands.title">Ten commands. Full control.</h2>
```

- [ ] **Step 2: Add 4 new cmd-rows after the merge row**

Find the closing `</div>` of the merge row:
```html
      <div class="cmd-row fade-up"><span class="cmd-tag">/skill-git:merge</span><span class="cmd-desc" data-i18n="cmd.merge">Combine two similar skills into one. Conflicts resolved interactively. Nothing written until you confirm.</span></div>
    </div>
```
Replace with:
```html
      <div class="cmd-row fade-up"><span class="cmd-tag">/skill-git:merge</span><span class="cmd-desc" data-i18n="cmd.merge">Combine two similar skills into one. Conflicts resolved interactively. Nothing written until you confirm.</span></div>
      <div class="cmd-row fade-up"><span class="cmd-tag">/skill-git:search</span><span class="cmd-desc" data-i18n="cmd.search">Discover skills from SkillHub and ClawHub by keyword or similarity to your existing skills.</span></div>
      <div class="cmd-row fade-up"><span class="cmd-tag">/skill-git:install</span><span class="cmd-desc" data-i18n="cmd.install">Install a skill from SkillHub or ClawHub with one command. Detects conflicts before writing anything.</span></div>
      <div class="cmd-row fade-up"><span class="cmd-tag">/skill-git:list</span><span class="cmd-desc" data-i18n="cmd.list">List all installed skills with their current versions and registration status.</span></div>
      <div class="cmd-row fade-up"><span class="cmd-tag">/skill-git:delete</span><span class="cmd-desc" data-i18n="cmd.delete">Permanently remove a skill from disk and config. Warns if uncommitted changes exist.</span></div>
    </div>
```

- [ ] **Commit**

```bash
git add index.html
git commit -m "feat: add search/install/list/delete to commands section"
```

---

### Task 2: Update Features grid — layout + 2 new cards

**Files:**
- Modify: `index.html` (CSS `.features-grid` ~line 126, features grid HTML ~line 260–295)

- [ ] **Step 1: Update CSS grid columns**

Find:
```css
    .features-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 16px; margin-bottom: 56px; }
```
Replace with:
```css
    .features-grid { display: grid; grid-template-columns: repeat(4,1fr); gap: 16px; margin-bottom: 56px; }
```

- [ ] **Step 2: Add 2 new feature cards at the end of the grid**

Find the closing tags of the last feature card (feat6 / Portable):
```html
      </div>
    </div>
    <div class="teaser-wrap fade-up">
```
Replace with:
```html
      </div>
      <div class="feature-card fade-up">
        <div class="feature-icon">🔍</div>
        <h3 data-i18n="feat7.title">Discover &amp; Install</h3>
        <p data-i18n="feat7.desc">Search SkillHub and ClawHub by keyword — install in one command with automatic conflict detection.</p>
      </div>
      <div class="feature-card fade-up">
        <div class="feature-icon">🗑️</div>
        <h3 data-i18n="feat8.title">Library Management</h3>
        <p data-i18n="feat8.desc">List, inspect, and delete skills. Keep your library lean and intentional.</p>
      </div>
    </div>
    <div class="teaser-wrap fade-up">
```

- [ ] **Commit**

```bash
git add index.html
git commit -m "feat: add Discover & Install and Library Management feature cards (4-col grid)"
```

---

### Task 3: Update Best Practices — new workflow + update workflow 3

**Files:**
- Modify: `index.html` (Best Practices section ~line 361–390)

- [ ] **Step 1: Insert new workflow card at the top of `.workflow-list`**

Find:
```html
    <div class="workflow-list">
      <div class="workflow-card fade-up">
        <h3 data-i18n="bp.w1.title">🚀 Version-control your skill library from scratch</h3>
```
Replace with:
```html
    <div class="workflow-list">
      <div class="workflow-card fade-up">
        <h3 data-i18n="bp.w0.title">🔍 Find, install, and version-control a new skill</h3>
        <p data-i18n="bp.w0.desc">You want to add a skill but aren't sure what's out there.</p>
        <pre>/skill-git:search I want to do code review
/skill-git:install clawhub:code-review
/skill-git:commit</pre>
        <p class="workflow-result" data-i18n="bp.w0.result">→ search returns top results from SkillHub and ClawHub ranked by relevance. install drops files into ~/.claude/skills/ tagged v1.0.0 automatically. You're version-controlled from day one.</p>
      </div>
      <div class="workflow-card fade-up">
        <h3 data-i18n="bp.w1.title">🚀 Version-control your skill library from scratch</h3>
```

- [ ] **Step 2: Update workflow 3 (Merge) to include delete step**

Find the existing workflow 3 code block and result:
```html
        <pre>/skill-git:scan code-review critic
/skill-git:merge code-review critic</pre>
        <p class="workflow-result" data-i18n="bp.w3.result">→ scan rates the overlap (★★★ / ★★☆ / ★☆☆). merge combines them interactively — nothing is written until you confirm.</p>
```
Replace with:
```html
        <pre>/skill-git:scan
/skill-git:merge code-review critic
/skill-git:commit
/skill-git:delete critic</pre>
        <p class="workflow-result" data-i18n="bp.w3.result">→ scan rates the overlap (★★★ / ★★☆ / ★☆☆). merge combines them interactively — nothing is written until you confirm. delete removes the now-redundant original after you confirm.</p>
```

- [ ] **Commit**

```bash
git add index.html
git commit -m "feat: add search+install best practice; update merge workflow to include delete"
```

---

### Task 4: Update i18n dictionary — English

**Files:**
- Modify: `index.html` (JS `dict.en` object ~line 401–441)

- [ ] **Step 1: Update `commands.title` in `en`**

Find in `dict.en`:
```js
      'commands.label': 'Commands', 'commands.title': 'Six commands. Full control.',
```
Replace with:
```js
      'commands.label': 'Commands', 'commands.title': 'Ten commands. Full control.',
```

- [ ] **Step 2: Add new command keys after `cmd.merge` in `en`**

Find:
```js
      'cmd.merge': 'Combine two similar skills into one. Conflicts resolved interactively. Nothing written until you confirm.',
```
Replace with:
```js
      'cmd.merge': 'Combine two similar skills into one. Conflicts resolved interactively. Nothing written until you confirm.',
      'cmd.search': 'Discover skills from SkillHub and ClawHub by keyword or similarity to your existing skills.',
      'cmd.install': 'Install a skill from SkillHub or ClawHub with one command. Detects conflicts before writing anything.',
      'cmd.list': 'List all installed skills with their current versions and registration status.',
      'cmd.delete': 'Permanently remove a skill from disk and config. Warns if uncommitted changes exist.',
```

- [ ] **Step 3: Add feat7/feat8 keys in `en`**

Find:
```js
      'feat6.title': 'Portable', 'feat6.desc': 'Each skill folder is self-contained. Move or share it — the full history travels with it.',
```
Replace with:
```js
      'feat6.title': 'Portable', 'feat6.desc': 'Each skill folder is self-contained. Move or share it — the full history travels with it.',
      'feat7.title': 'Discover & Install', 'feat7.desc': 'Search SkillHub and ClawHub by keyword — install in one command with automatic conflict detection.',
      'feat8.title': 'Library Management', 'feat8.desc': 'List, inspect, and delete skills. Keep your library lean and intentional.',
```

- [ ] **Step 4: Add bp.w0 keys and update bp.w3.result in `en`**

Find:
```js
      'bp.w1.title': '🚀 Version-control your skill library from scratch',
```
Replace with:
```js
      'bp.w0.title': '🔍 Find, install, and version-control a new skill',
      'bp.w0.desc': "You want to add a skill but aren't sure what's out there.",
      'bp.w0.result': '→ search returns top results from SkillHub and ClawHub ranked by relevance. install drops files into ~/.claude/skills/ tagged v1.0.0 automatically. You\'re version-controlled from day one.',
      'bp.w1.title': '🚀 Version-control your skill library from scratch',
```

Find:
```js
      'bp.w3.result': '→ scan rates the overlap (★★★ / ★★☆ / ★☆☆). merge combines them interactively — nothing is written until you confirm.',
```
Replace with:
```js
      'bp.w3.result': '→ scan rates the overlap (★★★ / ★★☆ / ★☆☆). merge combines them interactively — nothing is written until you confirm. delete removes the now-redundant original after you confirm.',
```

- [ ] **Commit**

```bash
git add index.html
git commit -m "feat: update en i18n for v1.1 commands, features, best practices"
```

---

### Task 5: Update i18n dictionary — Chinese

**Files:**
- Modify: `index.html` (JS `dict.zh` object ~line 442–482)

- [ ] **Step 1: Update `commands.title` in `zh`**

Find in `dict.zh`:
```js
      'commands.label': '命令', 'commands.title': '六条命令，完全掌控',
```
Replace with:
```js
      'commands.label': '命令', 'commands.title': '十条命令，完全掌控',
```

- [ ] **Step 2: Add new command keys after `cmd.merge` in `zh`**

Find in `dict.zh`:
```js
      'cmd.merge': '将两个相似技能合并为一个，交互式解决冲突，确认前不写入任何内容。',
```
Replace with:
```js
      'cmd.merge': '将两个相似技能合并为一个，交互式解决冲突，确认前不写入任何内容。',
      'cmd.search': '通过关键词或与现有技能的相似度，从 SkillHub 和 ClawHub 发现新技能。',
      'cmd.install': '一条命令从 SkillHub 或 ClawHub 安装技能，写入前自动检测冲突。',
      'cmd.list': '列出所有已安装的技能及其当前版本和注册状态。',
      'cmd.delete': '从磁盘和配置中永久删除技能，存在未提交变更时会发出警告。',
```

- [ ] **Step 3: Add feat7/feat8 keys in `zh`**

Find in `dict.zh`:
```js
      'feat6.title': '便携性', 'feat6.desc': '每个技能文件夹完全自包含，移动或分享时完整历史随行。',
```
Replace with:
```js
      'feat6.title': '便携性', 'feat6.desc': '每个技能文件夹完全自包含，移动或分享时完整历史随行。',
      'feat7.title': '发现与安装', 'feat7.desc': '通过关键词搜索 SkillHub 和 ClawHub — 一条命令安装，自动检测冲突。',
      'feat8.title': '库管理', 'feat8.desc': '列出、检查和删除技能，保持技能库精简、有条不紊。',
```

- [ ] **Step 4: Add bp.w0 keys and update bp.w3.result in `zh`**

Find in `dict.zh`:
```js
      'bp.w1.title': '🚀 从零开始对技能库进行版本控制',
```
Replace with:
```js
      'bp.w0.title': '🔍 发现、安装并对新技能进行版本控制',
      'bp.w0.desc': '你想添加一个技能，但不确定有哪些选择。',
      'bp.w0.result': '→ search 按相关度返回 SkillHub 和 ClawHub 的最佳结果。install 将文件放入 ~/.claude/skills/ 并自动标记为 v1.0.0，从第一天起即受版本控制。',
      'bp.w1.title': '🚀 从零开始对技能库进行版本控制',
```

Find in `dict.zh`:
```js
      'bp.w3.result': '→ scan 对重叠程度评级（★★★ / ★★☆ / ★☆☆），merge 交互式合并 — 确认前不写入任何内容。',
```
Replace with:
```js
      'bp.w3.result': '→ scan 对重叠程度评级（★★★ / ★★☆ / ★☆☆），merge 交互式合并 — 确认前不写入任何内容。delete 在你确认后删除已冗余的原始技能。',
```

- [ ] **Commit**

```bash
git add index.html
git commit -m "feat: update zh i18n for v1.1 commands, features, best practices"
```

---

## Self-Review

**Spec coverage:**
- ✅ Commands title updated (Task 1)
- ✅ 4 new cmd-rows: search, install, list, delete (Task 1)
- ✅ Features grid: repeat(4,1fr) (Task 2)
- ✅ feat7 Discover & Install card (Task 2)
- ✅ feat8 Library Management card (Task 2)
- ✅ Best Practices new bp.w0 workflow (Task 3)
- ✅ Workflow 3 updated with delete step (Task 3)
- ✅ en i18n: all new keys (Task 4)
- ✅ zh i18n: all new keys (Task 5)

**Placeholder scan:** No TBDs or TODOs found.

**Type consistency:** All i18n keys referenced in HTML (`data-i18n="..."`) match keys defined in Tasks 4–5.
