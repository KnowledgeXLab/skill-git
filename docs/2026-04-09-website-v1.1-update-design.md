# Website v1.1 Update Design

**Date:** 2026-04-09  
**Branch:** gh-pages  
**Reference:** main branch README (v1.1)

## Overview

Update the skill-git landing page (`index.html`) to reflect the 4 new commands shipped in v1.1: `search`, `install`, `list`, `delete`.

Three sections need changes: Commands, Features, Best Practices. The i18n dictionary (en + zh) must be updated in sync with all HTML changes.

---

## 1. Commands Section

**Title change:** `"Six commands. Full control."` → `"Ten commands. Full control."`

**Add 4 new `cmd-row` entries** after `/skill-git:merge`:

| Command | Description (EN) |
|---------|-----------------|
| `/skill-git:search` | Discover skills from SkillHub and ClawHub by keyword or similarity. |
| `/skill-git:install` | Install a skill from SkillHub or ClawHub with one command, with conflict detection. |
| `/skill-git:list` | List all installed skills with their current versions. |
| `/skill-git:delete` | Permanently remove a skill from disk and config. Asks for confirmation. |

i18n keys: `cmd.search`, `cmd.install`, `cmd.list`, `cmd.delete`  
i18n title key: `commands.title` updated in both `en` and `zh`.

---

## 2. Features Section

**Grid layout change:** `repeat(3, 1fr)` → `repeat(4, 1fr)`

**Responsive breakpoints:**
- ≤ 800px: `repeat(2, 1fr)` (was already 2-col at this breakpoint — no change)
- ≤ 640px: `1fr` (no change)

**Add 2 new feature cards** at the end of the grid:

| # | Icon | Title | Description |
|---|------|-------|-------------|
| feat7 | 🔍 | Discover & Install | Search SkillHub and ClawHub by keyword — install in one command with conflict detection. |
| feat8 | 🗑️ | Library Management | List, inspect, and delete skills. Keep your library lean and intentional. |

i18n keys: `feat7.title`, `feat7.desc`, `feat8.title`, `feat8.desc`

---

## 3. Best Practices Section

**Add 1 new workflow card** at the top of the list (before the existing 4):

- **Title:** `🔍 Find, install, and version-control a new skill`
- **Description:** You want to add a skill but aren't sure what's out there.
- **Code block:**
  ```
  /skill-git:search I want to do code review
  /skill-git:install clawhub:code-review
  /skill-git:commit
  ```
- **Result:** → search returns top results from SkillHub and ClawHub. install drops files into `~/.claude/skills/` tagged `v1.0.0` automatically.

**Update existing workflow 3** (Merge two overlapping skills) — append a `delete` step to match the README example:
```
/skill-git:scan
/skill-git:merge code-review critic
/skill-git:commit
/skill-git:delete critic
```
Result text updated to mention `delete` removes the now-redundant original.

i18n keys: `bp.w0.title`, `bp.w0.desc`, `bp.w0.result`; updated `bp.w3.*`

---

## 4. i18n Dictionary

All new keys added to both `en` and `zh` objects:

- `commands.title`: "Ten commands. Full control." / "十条命令，完全掌控"
- `cmd.search`, `cmd.install`, `cmd.list`, `cmd.delete`
- `feat7.title`, `feat7.desc`, `feat8.title`, `feat8.desc`
- `bp.w0.title`, `bp.w0.desc`, `bp.w0.result`
- Updated `bp.w3.*` to include delete step

---

## Out of Scope

- No changelog section added to the website (not in current design)
- No changes to hero subtitle, install commands, or nav links
- No changes to footer
